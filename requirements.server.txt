fastapi==0.128.1
uvicorn==0.40.0
gunicorn==25.1.0

python-dotenv==1.2.2
requests==2.32.5
PyMySQL==1.1.2
python-multipart==0.0.22

# openai는 helper에서 쓰면 유지
openai==2.8.1

# CORS를 쓰면 유지(코드에서 import 할 때만)
flask-cors==6.0.2

# 메일/검증을 쓰면 유지(코드에서 import 할 때만)
email-validator==2.3.0
dnspython==2.8.0

# DB
sqlalchemy==2.0.48
cryptography

# Vision
opencv-python-headless==4.12.0.88
deepface==0.0.99
numpy==2.2.6
pillow==12.1.1

# TensorFlow / Keras (DeepFace 의존)
tensorflow==2.20.0
tf-keras==2.20.1

# WebSocket (uvicorn용)
websockets==15.0.1